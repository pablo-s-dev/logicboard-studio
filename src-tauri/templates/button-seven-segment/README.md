# Buttons and seven-segment

Press Run and hold a pushbutton to display digits 1–4 on `HEX0`. The matching green status LED turns on. Both the buttons and seven-segment display are active-low, while the LEDs are active-high.
