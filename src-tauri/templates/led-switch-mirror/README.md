# LED and switch mirror

Press Run, then toggle `SW0` through `SW9`. Each switch is combinationally mirrored to its matching red LED. This is the smallest example of vector ports and vector board mappings.
